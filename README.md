AI-powered Python code explainer and flowchart visualizer.

## Stack
- Backend: FastAPI + Python AST → structured explanation + Mermaid diagram
- Frontend: React (Vite) + Mermaid.js

## Run locally
Backend:
```bash
uvicorn backend.main:app --reload
